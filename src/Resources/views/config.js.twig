window.HuhContaoPwaBundle = {
    debug: {% if debug|default(false) %}true{% else %}false{% endif %},
    serviceWorker: {
        path: '{{ serviceWorkerPath }}'
    },
    pushNotifications: {
        support: {% if supportPush|default(false) %}true{% else %}false{% endif %},
        subscribePath: '{{ subscribePath }}',
        unsubscribePath: '{{ unsubscribePath }}'
    }
};